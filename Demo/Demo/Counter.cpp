/*
#include <iostream>
using namespace std;

class Counter
{
private:
    static int count; // Static member variable to keep track of the count

public:
    // Static member function to increment the count
    static void increment()
    {
        count++;
    }

    // Static member function to get the current count
    static int getCount()
    {
        return count;
    }
};

// Definition of the static member variable
int Counter::count = 0;

int main()
{
    // Call static member functions using the class name
    Counter::increment();
    Counter::increment();
    Counter::increment();

    // Get the current count using the static member function
    cout << "Current count: " << Counter::getCount() << endl;

    return 0;
}
*/
